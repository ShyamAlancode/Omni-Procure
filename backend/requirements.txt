fastapi>=0.104.0
uvicorn>=0.23.2
pydantic>=2.4.2
SQLAlchemy>=2.0.23
python-dotenv>=1.0.0

# LLM and AWS Primitives
boto3>=1.34.0
botocore>=1.34.0

# Make sure you install the exact internal SDKs for nova-act and Strands
# as per your Amazon hackathon specific access constraints.
# e.g., amazon-nova-act, aws-strands-agents

# MCP
mcp>=1.0.0
websockets>=12.0
